# 3.6 Write a program that receives an ASCII code (an integer between 0 and 127) 
# and displays its character

# Prompt the user to enter an ASCII code
asciiCode = eval(input("Enter an ASCII code: "))

# Display the character for the ascii code
print("The character is", chr(asciiCode))
